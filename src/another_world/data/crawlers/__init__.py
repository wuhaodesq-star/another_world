"""Video / web crawlers.

Stage 1.2 of the roadmap will populate this module with:

- ``youtube.py``       :  yt-dlp wrapper with CC license filtering.
- ``vimeo.py``         :  Vimeo CC license fetcher.
- ``manifest.py``      :  Deterministic shard manifests for resumable crawls.

Each crawl batch must be explicitly approved by the project owner before
execution (proxy / quota / target list confirmation).
"""
